deployment_name: fixture-tls-acme-http01
domain:
  base_domain: github.example.com
  mode: flat-siblings
deployment:
  platform: plain-nginx
tls:
  mode: acme-http01
paths:
  error_root: /tmp/errors
  log_dir: /tmp/logs
  output_dir: ./dist/fixture-tls-acme-http01
nginx:
  snippets_target_hint: /tmp/snippets
  vhost_target_hint: /tmp/conf.d
